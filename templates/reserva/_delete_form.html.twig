<form method="post" action="{{ path('app_reserva_delete', {'idReserva': reserva.idReserva}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reserva.idReserva) }}">
    <button class="btn">Delete</button>
</form>
