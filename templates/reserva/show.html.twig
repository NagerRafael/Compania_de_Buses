{% extends 'base.html.twig' %}

{% block title %}Reserva{% endblock %}

{% block body %}
    <h1>Reserva</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdReserva</th>
                <td>{{ reserva.idReserva }}</td>
            </tr>
            <tr>
                <th>Observacion</th>
                <td>{{ reserva.observacion }}</td>
            </tr>
            <tr>
                <th>EstadoReserva</th>
                <td>{{ reserva.estadoReserva }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ reserva.estado }}</td>
            </tr>
            <tr>
                <th>Horario</th>
                <td>{{ reserva.horario ? reserva.horario|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reserva_index') }}">back to list</a>

    <a href="{{ path('app_reserva_edit', {'idReserva': reserva.idReserva}) }}">edit</a>

    {{ include('reserva/_delete_form.html.twig') }}
{% endblock %}
