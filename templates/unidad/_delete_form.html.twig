<form method="post" action="{{ path('app_unidad_delete', {'idUnidad': unidad.idUnidad}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ unidad.idUnidad) }}">
    <button class="btn">Delete</button>
</form>
